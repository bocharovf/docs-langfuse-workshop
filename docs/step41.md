# Сценарий

Установите в `True` только указанные переменные окружения в `.env`:

```
STEP_4 = True
```

```
docker compose up -d --force-recreate mcp-server
docker compose up -d --force-recreate cookbook-agent
```

## Задача

1. Протестировать агент и определить проблему.
2. Провести диагностику проблемы и найти корневую причину.

**Знаешь ответ - подними руку!**